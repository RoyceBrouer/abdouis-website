import React from "react";
import styles from "./ChangeWorkshopButton.module.css";

export default function ChangeWorkshopButton() {
  return (
    <button className={`${styles.change_workshop_button}`}>
      change image and announcement text
    </button>
  );
}
