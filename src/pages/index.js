import Head from "next/head";
import Image from "next/image";
import { Autour_One, Inter } from "next/font/google";
import styles from "../styles/Homepage/styles.css";
import photo from "../temporary-resources/images/photo1.jpg";

const inter = Inter({ subsets: ["latin"] });

export default function Home() {
  function pickRandomImage(array) {
    // number between 0 and array.length
    return Math.floor(Math.random() * array.length);
  }

  // const image = {
  //   url: "temporary-resources/images/2020.09_Prof.LazaridesTeam_TobiasHopfgarten__DSC6183.jpg",
  //   dimensions: { height: 3280, width: 4928 },
  // };

  // const { dimensions } = image;

  const aspectRatio = (photo) => {
    photo.width / photo.height;
  };
  const calculatedWidth = 500 * aspectRatio; //500 is the height I give it

  return (
    <>
      <Head>
        <title>F. Abdoui Mohamed</title>
        {/* <meta name="description" content="Generated by create next app" /> */}
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className={`${styles.main} ${inter.className}`}>
        <div className={`imagebox`}>
          <Image
            src={photo}
            alt="Profile Picture"
            className={`homeImage`}
            // width={calculatedWidth}
            // height="500"
            // fill={true}
          />
        </div>
      </main>
    </>
  );
}
